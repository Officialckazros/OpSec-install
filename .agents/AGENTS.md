# Workspace Rules

- All scripts, source files, and configuration files must be written completely clean without any inline comments (`# ...`, `// ...`, `<!-- ... -->`), explanations, or AI code traces.
- Always deploy changes to GitHub after modifying code in this repository.
